# LITERATURE.md — moved

The reading list has been **reconciled into a single source of truth**:

➡️ **[`paper-library/INDEX.md`](paper-library/INDEX.md)**

That file now holds everything this one used to — the curated rationale, the per-item "why",
the suggested reading order — merged with the physical library (what we have vs. what to acquire,
each item tagged with the Vega module it feeds). The old `TO_ACQUIRE.md` is folded in there too
(the `⬜` rows + the Acquisition-links appendix).

Equation/notation sheets extracted from these papers live in
[`paper-library/notation/`](paper-library/notation/).

> Kept as a stub because other docs link here. Don't add new reading to this file — add it to INDEX.
